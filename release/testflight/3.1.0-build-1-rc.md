# eSheep+ 3.1.0 (1) 内部 TestFlight RC 记录

## 构建绑定

- RC Git commit：`08b1de1922d141cea40a3fa83df2d2d46250152e`
- 分支：`codex/3.1-release`
- Marketing Version：`3.1.0`
- Build：`1`；仅在 App Store Connect 已占用时改为 `2`
- Xcode：27 Beta 4 (`27A5228h`)
- App Bundle ID：`com.sheepfarm.ios`
- Widget Bundle ID：`com.sheepfarm.ios.widget`
- Team：`7BBQMZ87SM`
- Supabase Free project ref：`rnqrvthbunrzqtprquqx`
- Release 环境：`APP_ENVIRONMENT=production`、`SUPABASE_ENABLED=YES`、`SUBSCRIPTIONS_ENABLED=NO`

## 2026-08-18 自动化证据

- 仓库外预上传备份：`/Volumes/移动硬盘/Codex/Backups/eSheepNext/20260818-161316-3.1-testflight-preflight`
- 内部 TestFlight 硬门禁：通过；Release App、Widget、App Intents 编译成功。
- 定向 XCTest：64/64 通过；TMR 15/15 通过。
- 全量串行 XCTest：发现 545 项，544 通过、0 失败、1 项由测试源码主动跳过。
- 跳过项：`CloudCollaborationTests.testOneTimeInvitationParticipantIsPreparedOnMainActor`；原因是 iOS 27 Beta CloudKit 系统陷阱，必须由已签名真机路径补验。
- 本地 Supabase：20 个迁移从空库完整重放成功。
- pgTAP：4 个文件、70 项全部成功；本地 `supabase_admin` 默认权限差异保留为明确 TODO，不影响 `anon`/`authenticated` 应用角色断言。
- Database Advisor：Security 0 项，Performance 0 项。
- Database lint：0 error；`stage_farm_projection_batch.v_transition` 有 1 项未读取变量 warning。
- `git diff --check`：通过。
- 暂存内容凭据扫描：未发现 service-role key、数据库密码、SMTP 密码或私钥。

## 当前阻塞

- Xcode 27 Beta GUI 已登录 `1196705821@qq.com`，团队为 `Jianxin Liu, Admin`；首次命令行自动签名曾错误报告 `No Accounts`，打开账号页并刷新 profile 后主 App 不再报告该错误。
- Apple Developer 后台已存在 `com.sheepfarm.ios.widget`，并已关联 `group.com.sheepfarm`；当前缺少该单数 Widget App ID 的本地 provisioning profile。本机只有旧的复数 `com.sheepfarm.ios.widgets` profile，不能用于当前 Target。
- iPhone 16 Pro 已配对并一度显示 available，但尚未完成已签名 Release 安装和可视化主路径。
- 尚未生成、Validate 或上传 Archive；App Store Connect App 记录和构建号占用情况尚未核对。
- 远程 Supabase Free 项目尚未执行逻辑备份、对象清单、迁移对账或只向前 push；本轮没有修改远程数据库。
- 本地 Edge Runtime 2.114.0 对 `account-deletion-worker` 报入口解析失败；删除请求 RPC 和数据库测试已通过，Worker 按计划不阻塞首次内部 TestFlight，外部 TestFlight 前必须修复。

## 上传后必须补写

- Archive 绝对路径与 SHA-256：待生成。
- Archive 实际签名 entitlement/profile 核对：待完成。
- App Store Connect App ID：待核对或创建。
- 上传时间、处理状态和最终构建号：待完成。
- `3.1 Internal` 组与测试账号：待完成。
- TestFlight iPhone 16 Pro 安装、冷启动、Supabase 主路径、重装恢复、Widget 和 Outbox 记录：待完成。
