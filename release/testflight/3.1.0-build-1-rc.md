# eSheep+ 3.1.0 (1) 内部 TestFlight RC 记录

## 构建绑定

- RC Git commit：`08b1de1922d141cea40a3fa83df2d2d46250152e`
- 分支：`codex/3.1-release`
- Marketing Version：`3.1.0`
- Build：`1`；仅在 App Store Connect 已占用时改为 `2`
- Xcode：27 Beta 4 (`27A5228h`)
- App Bundle ID：`com.sheepfarm.ios`
- Widget Bundle ID：`com.sheepfarm.ios.widget`
- Team：`7BBQMZ87SM`
- Supabase Free project ref：`rnqrvthbunrzqtprquqx`
- Release 环境：`APP_ENVIRONMENT=production`、`SUPABASE_ENABLED=YES`、`SUBSCRIPTIONS_ENABLED=NO`

## 2026-08-18 自动化证据

- 仓库外预上传备份：`/Volumes/移动硬盘/Codex/Backups/eSheepNext/20260818-161316-3.1-testflight-preflight`
- 内部 TestFlight 硬门禁：通过；Release App、Widget、App Intents 编译成功。
- 定向 XCTest：64/64 通过；TMR 15/15 通过。
- 全量串行 XCTest：发现 545 项，544 通过、0 失败、1 项由测试源码主动跳过。
- 跳过项：`CloudCollaborationTests.testOneTimeInvitationParticipantIsPreparedOnMainActor`；原因是 iOS 27 Beta CloudKit 系统陷阱，必须由已签名真机路径补验。
- 本地 Supabase：20 个迁移从空库完整重放成功。
- pgTAP：4 个文件、70 项全部成功；本地 `supabase_admin` 默认权限差异保留为明确 TODO，不影响 `anon`/`authenticated` 应用角色断言。
- Database Advisor：Security 0 项，Performance 0 项。
- Database lint：0 error；`stage_farm_projection_batch.v_transition` 有 1 项未读取变量 warning。
- `git diff --check`：通过。
- 暂存内容凭据扫描：未发现 service-role key、数据库密码、SMTP 密码或私钥。
- Apple Developer 已创建 `eSheep+ 3.1 Widget Development`（UUID `22ca504f-cbaf-4a25-80f2-2564170ff3b7`）；Xcode 随后生成了精确匹配 `com.sheepfarm.ios.widget` 的托管开发 profile（UUID `28f25bf4-dd2c-4d50-a8c6-61fe9f0b9270`）。
- Release 真机签名构建：通过；App/Widget 均为精确 Bundle ID，App Group 均为 `group.com.sheepfarm`，主 App 包含 Apple 登录、CloudKit、WeatherKit 和 Push entitlement。
- iPhone 16 Pro 直装 Release：安装成功，`devicectl` 启动成功；Device Hub 画面读取超时，尚未记为可视化验收。
- Archive：成功；`/private/tmp/eSheepNext-3.1.0-1.xcarchive`，归档版本 `3.1.0 (1)`，Team `7BBQMZ87SM`。
- Archive 压缩包：`/private/tmp/eSheepNext-3.1.0-1.xcarchive.zip`，SHA-256 `716030e7eb35b64dd3a91d903396be12b6621cd9a538c7e3833cf3909519c222`。
- Apple Distribution：保留证书 SHA-1 `F582958AC7D4A8D376609D7AA89BB9C977D944EE`；重复证书已从 Apple Developer 后台撤销。
- App Store Profiles：主 App `eSheep+ 3.1 App Store`（UUID `34a1c124-b50d-42ac-bee2-50a590eb7937`）；Widget `eSheep+ 3.1 Widget App Store`（UUID `f78d0339-a82f-412d-acdc-cc24714c65b4`）。
- App Store IPA：`/private/tmp/eSheepNext-app-store-export-manual/eSheepNext.ipa`，SHA-256 `0bc954fbc9fa2a592abca3859f9af54df91937da7507ad63a38078737013fb46`。
- 分发签名核对：主 App `aps-environment=production`、CloudKit Production、`get-task-allow=false`；Widget `get-task-allow=false`；两者 App Group 均为 `group.com.sheepfarm`。
- Organizer Validate：通过，无错误、无警告。
- App Store Connect：App ID `6802611581`，名称 `eSheep+`，SKU `esheep-plus-ios`，主语言简体中文。
- 上传：`3.1.0 (1)` 已于 2026-08-18 18:13（Asia/Shanghai）由 Xcode 上传；App Store Connect 构建 ID `7bf192ac-17e3-46e4-8c00-d6b9358a7432`。
- 内部组：`3.1 Internal`，Group ID `6a190342-20e8-4d86-b18f-0e57c336d151`，自动分发关闭，已加入账号持有人 `1196705821@qq.com`。
- 出口合规：2026-08-18 18:21（Asia/Shanghai）已选择“`不属于上述的任意一种算法`”并保存；构建不再显示“缺少出口合规证明”，状态变为“准备提交”，有效期 90 天。
- 内部分发：构建 `3.1.0 (1)` 已关联 `3.1 Internal`；群组显示 1 个测试员、1 个构建版本，账号持有人状态为“已邀请”。
- 测试内容：已保存简体中文内部测试说明，覆盖 Supabase 登录与刷新、建场、圈舍与羊只、业务记录、TMR、照片、离线 Outbox、Widget 和账户删除请求。

## 当前阻塞

- Xcode 27 Beta GUI 已登录 `1196705821@qq.com`，团队为 `Jianxin Liu, Admin`；开发签名和 Archive 均已成功。
- Xcode 27 Beta CLI 的自动导出仍错误输出 `No Accounts`；已用 `release/testflight/ExportOptions-AppStore.plist` 明确映射保留证书和两个 Profile，手动导出与 Organizer 上传均成功。
- iPhone 16 Pro 已完成 Release 直装与进程启动，但 Device Hub 画面读取超时，尚未完成可视化首屏和主路径。
- TestFlight 构建处理、出口合规和内部组关联均已完成；账号持有人需在 iPhone 16 Pro 的 TestFlight 中接受邀请并安装，才能执行真机验收。
- 远程 Supabase Free 项目尚未执行逻辑备份、对象清单、迁移对账或只向前 push；本轮没有修改远程数据库。
- 本地 Edge Runtime 2.114.0 对 `account-deletion-worker` 报入口解析失败；删除请求 RPC 和数据库测试已通过，Worker 按计划不阻塞首次内部 TestFlight，外部 TestFlight 前必须修复。

## 上传后必须补写

- TestFlight iPhone 16 Pro 安装、冷启动、Supabase 主路径、重装恢复、Widget 和 Outbox 记录：待完成。
